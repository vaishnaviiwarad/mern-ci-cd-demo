#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/mnt/c/pnpm-store/browserslist@4.28.0/node_modules/browserslist/node_modules:/mnt/c/pnpm-store/browserslist@4.28.0/node_modules:/mnt/c/pnpm-store/node_modules"
else
  export NODE_PATH="/mnt/c/pnpm-store/browserslist@4.28.0/node_modules/browserslist/node_modules:/mnt/c/pnpm-store/browserslist@4.28.0/node_modules:/mnt/c/pnpm-store/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../../pnpm-store/browserslist@4.28.0/node_modules/browserslist/cli.js" "$@"
else
  exec node  "$basedir/../../../../../../../pnpm-store/browserslist@4.28.0/node_modules/browserslist/cli.js" "$@"
fi
